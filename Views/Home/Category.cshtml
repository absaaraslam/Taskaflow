@{
	ViewData["Title"] = "Category";
}



    @* <div class="container mt-5">
        <h2>Add Items</h2>
        <form id="itemForm">
            <div class="form-group">
                <label for="itemName">Category Name:</label>
                <input type="text" class="form-control" id="itemName" name="itemName" required>
            </div>
            <div class="form-group">
                <label for="itemQuantity">Quantity:</label>
                <input type="number" class="form-control" id="itemQuantity" name="itemQuantity" required>
            </div>
            <button type="button" class="btn btn-primary" id="addItemBtn">Add Category</button>
        </form>

        <hr>

        <h2>Items List</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Item Name</th>
                    <th>Quantity</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="itemList">
            </tbody>
        </table>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        $(document).ready(function () {
            // Initialize an empty array to store items
            var itemsArray = [];

            $("#addItemBtn").click(function () {
                debugger
                var itemName = $("#itemName").val();
                var itemQuantity = $("#itemQuantity").val();
                if (itemName != '' && itemQuantity != '') {
                    // Create an object to represent the item
                    var item = {
                        name: itemName,
                        quantity: itemQuantity
                    };

                    // Add the item to the array
                    itemsArray.push(item);

                    // Update the table with the new item


                    var html = '<tr data-index="' + (itemsArray.length - 1) + '">';
                    html += '<td>' + itemName + '</td>';
                    html += '<td>' + itemQuantity + '</td>';
                    html += '<td><button type="button" class="btn btn-danger deleteBtn">Delete</button></td>';
                    html += '</tr>';
                    $("#itemList").append(html);

                    // Clear input fields after adding item
                    $("#itemName").val('');
                    $("#itemQuantity").val('');
                } else {
                    alert("Please fill in all fields!");
                }
            });

            // Delete item
            $(document).on("click", ".deleteBtn", function () {
                debugger

                var rowIndex = $(this).closest('tr').data('index');
                // Remove the item from the array
                itemsArray.splice(rowIndex, 1);
                // Remove the row from the table
                $(this).closest('tr').remove();
            });
        });
    </script>
 *@
